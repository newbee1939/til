## Claude Code の Remote Control でスマホからローカルマシンの作業を継続可能に

https://dev.classmethod.jp/articles/claude-coderemotecontrol-enables-you-to-work-on-your-local-machine-from-your-smartphone/

## MCP Swarm

>MCP Swarm（Model Context Protocol Swarm）は、Anthropicが開発したModel Context Protocol (MCP) を活用し、複数のAIエージェントやツール（サーバ）を協調・連携させて動作させるフレームワークやアプローチのこと

## Google Cloud: Developer Knowledge MCP Server

https://www.publickey1.jp/blog/26/aigoogledeveloper_knowledge_api_mcp_servergo∫ogle.html

https://developers.google.com/knowledge/mcp

1. APIキーを作る

```shell
gcloud services enable developerknowledge.googleapis.com --project=PROJECT_ID
```

```shell
gcloud services api-keys create --project=PROJECT_ID --display-name="DK API Key"
```

2. Developer Knowledge MCP serverを有効化する

```shell
gcloud beta services mcp enable developerknowledge.googleapis.com \
    --project=PROJECT_ID
```

3. MCPの設定をする

```yaml
{
    "mcpServers": {
      "google-developer-knowledge": {
        "url": "https://developerknowledge.googleapis.com/mcp",
        "headers": {
          "X-Goog-Api-Key": "YOUR_API_KEY"
        }
      }
    }
}
```

## OTel: getActiveSpan

https://opentelemetry.io/ja/docs/languages/js/instrumentation/

```ts
const span = otelTrace.getActiveSpan();
if (span) {
  span.setStatus({ code: SpanStatusCode.ERROR, message });
  span.recordException(new Error(message));
}
```

- `getActiveSpan`で現在のアクティブなスパンを取得できる

## ⭐️OTelのspanのステータスについて

https://opentelemetry.io/ja/docs/concepts/signals/traces/#span-status

以下のいずれかの値を設定できる。

```ts
**
 * An enumeration of status codes.
 */
export declare enum SpanStatusCode {
    /**
     * The default status.
     */
    UNSET = 0,
    /**
     * The operation has been validated by an Application developer or
     * Operator to have completed successfully.
     */
    OK = 1,
    /**
     * The operation contains an error.
     */
    ERROR = 2
}
```

OTel Collectorのconfig内のルールで、このspanのステータスを見た上でバックエンドに流すトレースを選別することができる。

## OTel Collector Exporter debug

OTel Collectorのconfigでデバッグログを出す方法。

```yaml
exporters:
  # データソース: トレース、メトリクス、ログ
  # NOTE: v0.86.0 以前では `debug` ではなく `logging` とする
  debug:
    verbosity: detailed
```

参考: https://opentelemetry.io/ja/docs/collector/configuration/

## Service Workers

> Service Workerは、ブラウザがWebページとは別にバックグラウンドで実行するイベント駆動型のプログラマブルなプロキシスクリプトです。Webアプリケーション、ブラウザ、およびネットワークの間に位置し、ネットワークリクエストをインターセプトして制御する機能を提供します。

## xvfb

>Xvfb (X Virtual Framebuffer) は、ディスプレイや物理的な入力デバイスを持たないLinux環境（サーバなど）で、メモリ上に仮想的な画面（フレームバッファ）を作り出し、GUIアプリケーションを動作させるXサーバ

## Cloud Trace: トレース コンテキスト

https://docs.cloud.google.com/trace/docs/trace-context?hl=ja

>HTTP リクエストの場合、コンテキストの伝播は通常、traceparent ヘッダーや tracestate ヘッダーなどの HTTP ヘッダーを通じて行われます
>トレース コンテキストの伝播をサポートするGoogle Cloud サービスは通常、traceparent と以前の X-Cloud-Trace-Context ヘッダーの両方をサポートします
>可能な場合は、アプリケーションで traceparent ヘッダーを使用することをおすすめします

`X-Cloud-Trace-Context`はもう非推奨なのか？

## PostgreSQL: uuid-ossp

```sql
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
```

>PostgreSQLでRFC 4122準拠の様々なアルゴリズム（v1, v3, v4, v5）を用いて、一意なUUIDを生成する関数を提供する拡張モジュール

>主キーの生成などでランダムや時刻ベースのUUIDが必要な場合に利用され、CREATE EXTENSION "uuid-ossp";で導入して使う

## Cloudflare セットアップモード

https://www.mki.co.jp/lp/cloudflare/column_20200110.html

- Full Setup
  - 導入ドメインの権威DNSサーバがCloudflareになる
- CNAME Setup
  - 既設DNSサーバーが引き続き権威DNSとなる

基本的にフルセットアップの方がメリットが多い。そもそも、CNAME Setupはビジネスプランじゃないと使えないっぽい。

## TerraVision

>AI-Powered Terraform to Architecture Diagram Generator

https://github.com/patrickchugh/terravision

## terraform graphコマンド

Terraformに標準で組み込まれた、ビジュアライズ用のコマンド

## drawio-mcp

>CursorでTerraformの定義からdraw.ioの構成図を自動生成したい場合、**「MCP Tool Server」**をCursorに設定するのが最もスムーズで強力な方法です

以下のコマンドでdrawioの設定を作成

```shell
npx -y @drawio/mcp
```

https://github.com/jgraph/drawio-mcp#mcp-tool-server

## ⭐️Cloudflareの導入について
### 調査結果
- Freeプランではエッジ証明書の発行に`Universal SSL`を利用する必要がある
  - `Universal SSL`により、第1レベルサブドメインまでの証明書設定が可能になる
- ただし、Universal SSLを利用して証明書を発行するには、ネームサーバーの変更を完了しておく必要がある（Full Setup）
  - Cloudflareの「Full Setup（通常セットアップ）」で**Universal SSL（無料証明書）**を有効にするには、Cloudflareがそのドメインの「権威ネームサーバー」になることが絶対条件
  - 参考: For domains on a primary setup (full)1, your domain should automatically receive its Universal SSL certificate within 15 minutes to 24 hours of domain activation2.
    - https://developers.cloudflare.com/ssl/edge-certificates/universal-ssl/enable-universal-ssl/#full-dns-setup
- FreeプランだとFull Setupしか利用できない

## つまり
- 最初の段階でネームサーバーの切り替えは必須

### 切り替えるとしたら
1. お名前のexample.comの全てのレコードをCloudlfareのexample.comのDNSレコードに追加
2. Cloudflareの「プロキシ ステータス」は全て「DNSのみ」にしておく
  - Cloudflareを経由せずに、オリジンサーバーへ直接接続する（権威サーバーがお名前からCloudflareに変わっただけで、DNSの挙動は変わらない）
3. example.comのネームサーバーの設定をお名前からCloudflareに切り替える
4. Universal SSLにより第1レベルサブドメインまでの証明書が有効になるのを待つ
5. Cloudflareの「プロキシ ステータス」を「プロキシ済み」に変更する
  - Cloudflareが発行した証明書とCloudflareの設定を経由して、originにリクエストが通るようになる

一応、ダウンタイムはゼロにできそう。

## Cloudflare: プロキシ ステータス

https://developers.cloudflare.com/dns/proxy-status/

単純に、Cloudflareを経由するか、バイパスするかを選択できる。

## 同一レコードがある場合の優先度について

> 親ゾーン（例: example.jp）と子ゾーン（例: sub.example.jp）の両方に同じレコード（例: www.sub.example.jp のAレコード）が書かれている場合、**「子ゾーン」の設定が優先（正解）**とみなされます

## Cloudflare: Universal SSL

参考: https://developers.cloudflare.com/ssl/edge-certificates/universal-ssl/
参考: https://developers.cloudflare.com/ssl/edge-certificates/universal-ssl/enable-universal-ssl/

### 概要

>デフォルトでは、CloudflareはCloudflareに追加され有効化されたすべてのドメインに対して、無料の非共有で公的に信頼されたSSL証明書を発行し、更新します。

### Universal SSLがカバーする範囲

無料のUniversal SSLがカバーするのは以下の2パターン

- ルートドメイン（e.g. example.com）
- 第1レベルサブドメイン（e.g. *.example.com）

## Cloudflare: CNAME setup

https://developers.cloudflare.com/dns/zone-setups/partial-setup/

>CNAME設定（部分設定とも呼ばれる）を使用すると、プライマリかつ権威あるDNSプロバイダーを維持しながら、Cloudflareのリバースプロキシを利用できます。

>権威あるDNSプロバイダーを変更できない場合、個々のサブドメインのみをCloudflare経由でプロキシするためにこのオプションを使用します。プロキシ可能なDNSレコードタイプであるAレコード、AAAAレコード、CNAMEレコードを作成できます。

## ハーネスエンジニアリング

https://zenn.dev/hiruno_tarte/articles/agent-harness-engineering

>エージェントがミスや想定外の振る舞いをした時に、再発防止をするというシンプルなアイデアです。AIにセルフレビューと修正を行わせることで、最初の出力からベストに近いものを出させたいというわけですね

2つのやるべきこと:

>AGENTS.md の改善: エージェントが誤った振る舞いをする場合は、AGENTS.md を更新して再発を防止する
>ツール整備: スクリーンショット/テストランナーなど、エージェントが使用できる専用のツールを整備する。

>これらを通して、エージェント自身が自らの作業を検証できるようにすることで、より長時間の自律的実行が可能になります

## Cloud Traceで実現するアプリケーションパフォーマンスの可視化

https://techblog.zozo.com/entry/2025-cloud-trace-performance-tuning-article-ma

## Cloudflare: Operators and grouping symbols

セキュリティポリシーのカスタムルールの記法まとめ

https://developers.cloudflare.com/ruleset-engine/rules-language/operators/

Freeプランでは正規表現は使えないっぽいので注意。

## Accessing n8n MCP server

https://docs.n8n.io/advanced-ai/accessing-n8n-mcp-server/

>MCPクライアント（Claude、Lovableなど）やその他のAIツールが、あなたのn8nワークフローを発見し実行できるようにしましょう

## SIEM Integration

https://sdpf.ntt.com/services/docs/cdn-akamai/service-descriptions/SIEM_Integration.html

## Cloud Run: サービス間認証

https://docs.cloud.google.com/run/docs/authenticating/service-to-service?hl=ja

- `サービス アカウントを設定する`
  - サービス アカウントを設定するには、呼び出し元のサービス アカウントを受信側サービスのプリンシパルにすることで、呼び出し側サービスからのリクエストを受け入れるように受信側サービスを構成する
  - そのサービス アカウントに Cloud Run 起動元（roles/run.invoker）のロールを付与する
- `ID トークンの取得と構成`
  - Google によって署名された ID トークンを取得

## 406 Not Acceptable

https://developer.mozilla.org/ja/docs/Web/HTTP/Reference/Status/406

- クライアントエラーレスポンスステータスコード
- Webサーバーがクライアント（ブラウザやアプリ）の要求したデータ形式（Content-Typeなど）を提供できない場合に発生するクライアントエラー

## playwright-mcp: --allowed-hosts

https://github.com/microsoft/playwright-mcp#playwright-mcp

>このサーバーがサービスを提供できるホストのカンマ区切りリスト。デフォルトはサーバーがバインドされているホストです。ホストチェックを無効にするには '*' を渡してください

つまり、デフォルトだとlocalhost通信しか許可されていない。外部のhostからアクセスしたい場合はワイルドカードを設定する必要がある。

## Go: idtoken

Goで、GoogleのメタデータサーバーからIDトークンを取得したい場合に利用するライブラリ。

https://pkg.go.dev/google.golang.org/api/idtoken

## Cloud Run: IDトークンを取得する

ID トークンを取得する

https://docs.cloud.google.com/docs/authentication/get-id-token?hl=ja

## Cloud Run: サービス間認証

https://docs.cloud.google.com/run/docs/authenticating/service-to-service?hl=ja

認証が必要な場合、アクセス元に`run.invoker`のroleを付与するだけでなく、IDトークンも必要。

## 認証が必要な Cloud Run サービスにアクセスする方法まとめ

https://sterfield.co.jp/blog/cloud-run-auth/

## 最小権限の原則による Cloud Run のデプロイ保護

https://cloud.google.com/blog/ja/products/identity-security/securing-cloud-run-deployments-with-least-privilege-access/


## navigator.webdriver

https://developer.mozilla.org/ja/docs/Web/API/Navigator/webdriver

- webdriver は navigator インターフェイスの読み取り専用プロパティで、ユーザーエージェントが自動制御されているかどうかを示す
- 多くのWebサイトは、JavaScriptのnavigator.webdriverプロパティを使ってブラウザが自動化ツールによって制御されているかを検出する

## Playwright: storageState

https://playwright.dev/docs/api/class-browsercontext#browser-context-storage-state

>Returns storage state for this browser context, contains current cookies, local storage snapshot and IndexedDB snapshot.

## Akamai: Prove Domain Ownership

https://techdocs.akamai.com/property-mgr/docs/prove-domain-ownership

> プロパティホスト名として設定する各ドメインについて、Akamai への所有権証明が必要です。これによりセキュリティが強化され、他の顧客がお客様のドメインを使用して Akamai エッジネットワーク経由でトラフィックを配信することを防止します

## constraints/gcp.resourceLocations

https://docs.cloud.google.com/resource-manager/docs/organization-policy/defining-locations?hl=ja

- 組織内で利用可能なlocationを絞る
- e.g. アメリカ国内に絞りたい場合は`in:us-locations`を指定する
- 以下、サンプルコード（asiaとusに制限）

```tf
resource "google_project_organization_policy" "resource_locations" {
  project    = project_id
  constraint = "constraints/gcp.resourceLocations"

  list_policy {
    allow {
      values = ["in:asia-locations", "in:us-central1"]
    }
  }
}
```

## google_project_organization_policy

https://registry.terraform.io/providers/hashicorp/google/latest/docs/resources/google_project_organization_policy

- Google Cloud Projectに対する組織ポリシーを設定する

```tf
resource "google_project_organization_policy" "services_policy" {
  project    = "your-project-id"
  constraint = "serviceuser.services"

  list_policy {
    allow {
      all = true
    }
  }
}
```

## Model Garden モデルへのアクセスを制御する

https://docs.cloud.google.com/vertex-ai/generative-ai/docs/control-model-access?hl=ja

- 組織内の全てのユーザーに対して、Model Gardenで利用できるモデルを限定できる組織ポリシー
- フォルダやプロジェクトの単位で指定可能
- モデルに対するアクション（許可/拒否する操作）を設定できる
  - `predict`
    - ユーザーがマネージド API を使用してモデルに対してオンライン予測とバッチ予測を実行できるかどうかを指定
    - 要は、単純にモデルを使えるか
  - `deploy`
    - ユーザーが Google Cloudにモデルをデプロイできるかどうか
  - `tune`
    - ユーザーがモデルをチューニングできるかどうか

## Claude Code Security

https://www.publickey1.jp/blog/26/anthropicclaude_code_security.html

>Claude Code Securityはルールベースの静的解析ではなく、人間のセキュリティ研究者と同じようにコードを分析し、複雑な脆弱性も発見できる

ホワイトハッカーの需要とか落ちるのか？

## ⭐️MySQL 8.0 認証エラーの理由と解決策のまとめ

```
ERROR 2061 (HY000): Authentication plugin 'caching_sha2_password' reported error: Authentication requires secure connection.
```

`mysql -u test-user -h 127.0.0.1 -P 3306 -p`だとエラーになるが、
`mysql -u test-user -h 127.0.0.1 -P 3306 -p --ssl-mode=DISABLED --get-server-public-key`だとエラーにならない理由。

1. エラーの原因 (`ERROR 2061`)
MySQL 8.0 からデフォルトになった認証プラグイン `caching_sha2_password` は、セキュリティのためにパスワードをそのままネットワークに流すことを禁止している。そのため、以下のいずれかの方法でパスワードを保護する必要がある

- SSL/TLS を利用した通信全体の暗号化
- RSA 公開鍵を利用したパスワードのみの暗号化

`-h 127.0.0.1` は TCP 接続となるが、SSL が有効でない環境で、かつパスワードをどう暗号化するかの指示がなかったため、サーバー側が拒否したのがエラーの真相

2. 成功したコマンドの仕組み
2番目のコマンドが通ったのは、以下のオプションでパスワード保護の代わりの手段を確立したから。

- --ssl-mode=DISABLED: 通信全体の SSL 暗号化は行わないと宣言。
- --get-server-public-key: 通信全体は暗号化しない代わりに、サーバーから RSA 公開鍵をもらってパスワードだけを暗号化して送る。

この手順を踏むことで、セキュアな接続（Secure connection）という要件を最低限満たした扱いになり、ログインが可能になった。

## Cloud Run Worker Poolsを作成するリソース

```
google_cloud_run_v2_worker_pool
```

https://registry.terraform.io/providers/hashicorp/google/6.37.0/docs/resources/cloud_run_v2_worker_pool

サンプル:

```tf
resource "google_cloud_run_v2_worker_pool" "default" {
  name     = "cloudrun-worker-pool"
  location = "us-central1"
  deletion_protection = false
  launch_stage = "ALPHA"

  template {
    containers {
      image = "us-docker.pkg.dev/cloudrun/container/worker-pool"
    }
  }
}
```

## Cloudflare: X-Forwarded-Proto

https://developers.cloudflare.com/fundamentals/reference/http-headers/#x-forwarded-proto

Cloudflare側で設定値を変えることはできない。

## WebSocket: code=1006

> エラーメッセージ（code=1006）は、WebSocketの接続が確立できなかったか、途中で「異常終了（何らかの理由で切断された）」したことを示しています

## GCP Cloud Run で n8n が常に「Offline」になるバグ

https://zenn.dev/ddd0609/articles/6bfe517888c338

## Cloud Run（n8n）は、受け取ったリクエストの Host でルーティングする

実際のリクエストURLが正しくても、Hostヘッダーの値が違ったら404になることがある。

## cloudflared: Quic is has slower response times than http2 protocol (confirmed issue with cloudflare support)

https://github.com/cloudflare/cloudflared/issues/895

Quicだと遅い？

## Cloudflare: TUNNEL_TRANSPORT_PROTOCOL

https://developers.cloudflare.com/cloudflare-one/networks/connectors/cloudflare-tunnel/configure-tunnels/run-parameters/

cloudflaredとCloudflareのグローバルネットワークの接続のために使われるプロトコルを指定できる。

## ワーカープールを Cloud Run にデプロイする

https://docs.cloud.google.com/run/docs/deploy-worker-pools?hl=ja#gcloud

## Cloud Run worker poolsについて

https://blog.g-gen.co.jp/entry/cloud-run-worker-pools-explained

- 新しいCloud Runの実行モデル（現在はまだプレビュー）
- Cloud Run worker pools これまでの実行モデルと異なり、Pub/Sub や Kafka のようなメッセージキューなどに対して、アプリケーションが能動的にメッセージやタスクの取得を行う pull 型のワークロードを実行するモデルとなっている
- Cloud Run worker pools には実行をトリガーするためのエンドポイント、URL が無い
- Pub/Sub や Kafka などのメッセージキューに対して処理を行うために、コンテナインスタンスは常に起動されている
- したがって、HTTP リクエストの受信をトリガーとする Cloud Run services と異なり、デプロイするコンテナがポートでリクエストをリッスンする必要がない

## Cloudflare Tunnel で内部公開の Cloud Run にアクセスしてみる

https://developers.cyberagent.co.jp/blog/archives/61112/

Cloudflare Tunnel (以下 Tunnel)経由で接続する場合、以下のような流れになる:

```
User -> Internet -> Cloudflare Access -> Cloudflare Network -> cloudflared -> Application
```

- この記事の場合、Cloud Run Worker Poolにcloudflaredをデプロイしている
- サイドカーではなく
- おそらく、Cloudflare Tunnelの設定画面でCloud Runの公開URLを指定する必要があるので、サイドカーだと厳しいから？

## Cloudflare Tunnel: Published applications

https://developers.cloudflare.com/cloudflare-one/networks/connectors/cloudflare-tunnel/routing-to-tunnel/

## Cloudflare Tunnelの接続の仕組み

https://developers.cloudflare.com/cloudflare-one/networks/connectors/cloudflare-tunnel/

### 従来の方式
サーバーがポートを開けて待ち、ユーザーが外から接続する（インバウンド）。公開IPとファイアウォール開放が必要。

### Cloudflare Tunnel の方式
cloudflared が Cloudflare にこちらの接続を張り続ける（アウトバウンドのみ）。ユーザーは Cloudflare のエッジにアクセスし、Cloudflare がトンネル経由で cloudflared に転送、cloudflared が localhost のアプリに渡す。

### なぜ PORT を公開しなくてよいか
従来は「ユーザー → サーバーの窓」。Tunnel は「cloudflared → Cloudflare」の一方通行の線を張るだけなので、サーバー側で LISTEN は不要。

### 通信の流れ
ユーザー → Cloudflare → トンネル → cloudflared → localhost:443（n8n）

## 広範な OTLP 対応に向けて: Cloud Monitoring が OpenTelemetry Protocol 指標をサポート

https://cloud.google.com/blog/ja/products/management-tools/otlp-opentelemetry-protocol-for-google-cloud-monitoring-metrics

- Cloud MonitoringがOTLP指標をサポート
- 特定プロバイダに依存しないデータ送信パイプラインを実現
- Managed Service for Prometheusと同じ形式での保存によりコストを抑制
- コレクタを介さないSDKからの直接送信で運用コストを軽減
- 現在プレビュー版として提供を開始

以下のGoogle Cloud Observabilityに対する対応を拡張した感じか。

https://cloud.google.com/blog/ja/products/management-tools/opentelemetry-now-in-google-cloud-observability?hl=ja

Google Cloudも着実にOTelの標準に対応してきている。

## Anycastネットワーク

>世界中の複数拠点にあるサーバーが「同一のIPアドレス」を共有し、ユーザーからネットワーク的に最も近いサーバーへ自動的に接続させるルーティング技術

だからCloudflareのDNS（`1.1.1.1`）も速い。

## Cloudflare Tunnel 仕組み

### 全体像

1. サーバーに専用ソフト(cloudflared)を導入する
2. ソフトが内側からCloudflareへ接続を開始する
3. 外からのポート開放なしで通信路(トンネル)ができる
4. 作成されたトンネルと公開用ドメインを紐づける
5. ユーザーがそのドメインにアクセスする
6. Cloudflareがトンネルを通してサーバーへデータを届ける

### 内側から接続を開始する詳細な仕組み

1. cloudflaredが起動すると、まずCloudflareのAnycast IP（最寄りのデータセンター）へ接続を試みる
2. 標準的なHTTPS(443番ポート)やQUICを用いて、通常のWebブラウジングと同じ「外向き」のリクエストを送る
3. Cloudflareのネットワーク側で認証が行われ、正当なコネクタであると認識される
4. 認証後、サーバーとCloudflareの間に「常時接続された通信路」が確立される
5. この接続は一度確立されると維持され続け、サーバー側から定期的に生存確認（キープアライブ）が行われる
6. cloudflaredはCloudflareからの「ユーザーのリクエストが来た」という通知をこの通信路で待ち受ける

## QUIC

- QUICは、Googleが開発し2021年に標準化（RFC 9000）された、UDPをベースとする高速・安全な次世代トランスポートプロトコル
- TCPの信頼性とUDPの低遅延性を両立し、HTTP/3の基盤として、パケットロスに強く、通信の切り替えや暗号化ハンドシェイクの高速化を実現している
