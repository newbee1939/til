## ⭐️Cloud NAT と Private Google Access (PGA) の関係

- Cloud NAT は `*.googleapis.com` 宛トラフィックを NAT 翻訳しない。"PGA が有効ならそちらに譲る"仕様

> "Traffic sent to Google APIs and services are routed through Private Google Access even if the VM instance initiating the connections uses Public NAT." — [Cloud NAT overview](https://cloud.google.com/nat/docs/overview)

- PGA 経由のトラフィックの送信元 IP は VM の内部 IP (RFC1918) として Google API 側に見える

## Google API キーの IP 制限の制約

- API キーの "サーバー IP アドレス制限" は **public IP のみ** 受付。内部 IP (RFC1918) や `localhost` は登録不可

> "Internal IP addresses and `localhost` aren't supported." — [API key restrictions](https://cloud.google.com/docs/authentication/api-keys)

- そのため Cloud Run (外部 IP なし) + PGA 構成からの Google API 呼び出しは、API キー IP 制限とは原理的に共存できない

## Claudeアプリにログインできなかった問題が解消

- Macに辞書登録していたメールアドレスをコピペしていたのが原因だった...
- 手打ちじゃないとイベントが発火せずにSSOの画面に切り替わらなかった
- 今後同様の事象にハマった場合はコピペに頼らず直接入力する方針で

## Claude Code: セッションに名前を付ける

```shell
# セッション開始時に指定
claude --name hoge

# セッション開始後に変更
/rename fuga

# セッションを指定して再開
claude -r
```

## Claude Code: 過去のセッションを削除

Claude Codeの履歴は以下に保存されている。

```
~/.claude/projects/プロジェクト名/セッション
```

このファイルを消したら、過去の履歴も削除される。

全ての履歴を消したい場合は、以下のコマンド。

```
rm -rf ~/.claude/projects/
```

## ⭐️AI時代のエンジニアに必要なパラダイムシフト － 自己否定の先にあるもの

https://tech.nri-net.com/entry/ai_era_software_engineer_paradigm_shift

> 知識の蓄積量で勝負する時代は、終わりに近づいているのではないでしょうか

### 価値が残る、あるいは増していくもの

- 価値の源泉
  - 課題設定力・判断力・実行力（定義する・見抜く・届ける）
    - 「問いを立てる力」
- エンジニアの役割
  - AIを指揮して実現する人
- 評価の尺度
  - どれだけ大きな課題を解決できるか

### なぜ「問い」が重要か

- AIのアウトプット品質は、インプット品質（「問い」の精度や解像度）に大きく依存する

### 否定した価値観

- 「自分の手で書いたコードに価値がある」という信念
- 「深い技術知識を持っていることが優秀さの証明」という評価軸
- 「AIより自分の方がうまく書ける」という判断で手を動かしてしまう習慣

> 優秀さの新しい尺度は、AIを活用して成果を出せるかどうかに移りつつあるのではないでしょうか

### 「AIで差がつく技術」の進化 — プロンプト → コンテキスト → ハーネス／エンバイロメント

プロンプト・エンジニアリング -> コンテキスト・エンジニアリング -> ハーネスエンジニアリング & エンバイロメントエンジニアリング

## Load Shedding 概要

>システムが過負荷（限界以上のアクセス）になった際、システム全体のダウンを防ぐため、意図的に一部の処理やリクエストを拒否・破棄する障害回避の技術

## ロード シェディングを利用して想定以上のトラフィックをさばく

https://cloudplatform-jp.googleblog.com/2016/12/cre.html

## Cloudflare 便利機能

https://webnation.co.jp/%E4%BB%8A%E6%9B%B4%E3%81%A0%E3%81%91%E3%81%A9cloudflare%E3%81%AE%E4%BE%BF%E5%88%A9%E3%81%AA%E6%A9%9F%E8%83%BD%E3%82%92%E7%B4%B9%E4%BB%8B%EF%BC%81/

1. 画像の強制キャッシュ
静的コンテンツをキャッシュし、サーバー負荷の軽減とトラフィックの削減を実現する

2. SSLの無料提供
フレキシブルSSLを活用すれば、サーバー側が未対応でもSSL化とキャッシュを両立できる

3. WAF機能（Proプラン以上）
SQLインジェクションなどの攻撃を、サーバーに到達する前に遮断する

4. DNS機能
Web上で完結する管理画面により、設定や更新をスムーズに行える

5. ゼロトラスト（Cloudflare Access）
特定のメールアドレスを用いた2段階認証などを設定でき、リモートワーク時の安全なアクセス制限に役立つ

## AIコードエディタ「Cursor」がプラグイン拡張に対応　開発ライフサイクルをエディタ内で完結

https://atmarkit.itmedia.co.jp/ait/articles/2603/26/news055.html

## Cursor: セルフホスト型クラウドエージェント

https://cursor.com/ja/changelog

## Terraform concat

- Terraform の組み込み関数
- 複数のリスト（配列）を結合して1つのリストにするもの

具体例:

```tf
> concat(["a", ""], ["b", "c"])
[
  "a",
  "",
  "b",
  "c",
]
```

## GitHub Actions: セルフホステッド ランナー

https://docs.github.com/ja/actions/concepts/runners/self-hosted-runners

>セルフホステッド ランナーとは、GitHub で GitHub Actions のジョブを実行するために、お客様がデプロイして管理するシステム

>GitHub Actions と共に無料で利用できますが、ランナー マシンのメンテナンス コストは、お客様の負担となります

## GitHub Actions: Larger Runner

>Larger Runners は「標準ランナーの上位版」という位置づけで、GitHub がフルマネージドで提供しつつ、静的IP・大きいスペック・オートスケールなどの追加機能がある、というイメージ

`単にIP固定をしたいだけならこちらを使ったほうがいい`か？

以下、詳細:

https://docs.github.com/en/actions/concepts/runners/larger-runners

## Terraform variable sensitive

```tf
variable "sample" {
  description = "sample"
  type        = string
  sensitive   = true
}
```

`sensitive = true`は、Terraformがその変数の値をログや出力に表示しないようにするための設定。

具体的には以下の効果がある。

- terraform plan / terraform apply の出力で値が (sensitive value) とマスクされる
- terraform output でも非表示になる
- この変数を参照するリソースの属性も、plan 上ではマスクされる

## Terraform: ephemeral 変数

- Ephemeral valuesは、Stateファイル・planファイルに 保存されない 値

https://dev.classmethod.jp/articles/terraform-1-10-is-now-generally-available/

## GCS サーバー側暗号化（AES-256）

参考: https://docs.cloud.google.com/storage/docs/encryption/default-keys?hl=ja

暗号化はデフォルトで有効になっている。

- Cloud Storage は、Google が独自の暗号化データに使用するものと同じ堅牢な鍵管理システム（厳格なアクセス管理や監査を含む）を使用して、ユーザーに代わって暗号鍵を管理する
- Cloud Storage では AES-256 を使用して保存ユーザーデータを暗号化する

## git checkout -B

- `-b`
  - すでに同名のブランチがある場合にはエラーににある
- `-B`
  - 同名のブランチがあってもエラーにならない

## terraform show

terraform.stateファイルを元に現在のリソースの状態を参照するコマンド

## ⭐️Terraform: 既存環境のインポートについて

- すでに手動で設定済みの場合は cf-terraforming という公式ツールを使う
- これを使うと、既存のリソースからterraformのコード（HCL）とimport文を自動生成できる
- terraform import を手動で繰り返すのは時間がかかるため、ツール活用が必須

https://future-architect.github.io/articles/20230502a/

これ使えそう。Cloudflareの全てのリソースに対して対応しているわけではなさそうだけど...

## OTel Collector 経由で Claude Code のテレメトリを Google Cloud に送ったら一部ログが破棄された件

### 事象
- ローカルの Claude Code → GLB → Cloud Run 上の OTel Collector → telemetry.googleapis.com の構成でテレメトリ送信を試した。
- Collector のログに以下のエラーが継続的に出て、ログが破棄されていた。
  - `code = InvalidArgument`
  - `desc = Failed to parse attribute value to int64: ... string_value: "connected"` / `"failed"`

### 原因
- Google Cloud Telemetry API は、OTLP ログの属性キー `status` を LogEntry.httpRequest.status（数値）にマップする予約キーとして扱う。
- Claude Code の `claude_code.mcp_server_connection` イベントは仕様上 `status` 属性に `"connected"` / `"failed"` / `"disconnected"` の文字列を入れる。
- 両者の命名が衝突し、GCP 側で数値パースに失敗 → 該当リクエストのログが丸ごと破棄されていた。

### 対処
- Collector の logs パイプラインに transform プロセッサを追加し、エクスポート前に `status` 属性を `mcp.connection.status` にリネームして GCP のマッピング対象から外す。
- 値そのものは文字列として保持されるため、Cloud Logging でも引き続き参照可能。

### 学び・今後の注意
- OTel の attribute キーは自由に命名できるが、GCP Telemetry API 側に予約キー（status, latency, http.response.status_code, code.function.number など）が存在し、型解釈の衝突が起きうる。
- OTel ドキュメントの「承認済みのタイプ: 文字列、整数」は「任意の文字列 OR 整数」ではなく「数字を表す文字列 OR 整数」の意味だった。
- 計装側（Claude Code など）を直せない場合は、中継する Collector 側で吸収するのが現実解。
- 新たに `Failed to parse attribute value to int64` を観測したら、衝突キーを特定して同じ要領でリネームルールを追加していく。

### 参考
- GCP マッピング仕様: https://cloud.google.com/stackdriver/docs/reference/telemetry/otlp-log-record-to-log-entry
- Claude Code テレメトリ仕様: https://docs.claude.com/en/docs/claude-code/monitoring-usage

## xattr

### 概要
- macOSには Gatekeeper というセキュリティ機能があり、インターネットからダウンロードしたアプリに「隔離フラグ（quarantine）」を自動で付ける
- このフラグが付いたアプリをAppleが検証できなかった場合、「壊れている」として起動をブロックする
- xattr は extended attributes（拡張属性） を操作するコマンド
- macOSのファイルには通常のデータ以外に、メタ情報（拡張属性）を付けられる
- Gatekeeperの隔離フラグもその一つ

```shell
xattr -cr /Applications/Claude.app
```

- `-c`
  - そのファイル/ディレクトリの全拡張属性を削除
- `-r`
  - ディレクトリを再帰的に処理（.appの中身も含めて）

## google_logging_metric: resource.type="generic_task"

参考: https://docs.cloud.google.com/monitoring/api/resources

- 使用可能なモニタリング対象リソースタイプ（`Monitored resource types`）
  - 要は、Metrics Explorer などで選択できる`指標`のこと
- その中の一つが、`generic_task`
  - 特定の Google Cloud サービス（GKE や App Engine など）に該当しない、汎用的なアプリケーションプロセスを定義するためのリソースタイプ
- `generic_task`には5つの必須ラベルがある
  - project_id
  - location
  - namespace
  - job
  - task_id

> Logs Explorer は寛容なので namespace が空でもログを表示してくれます。でも Cloud Monitoring のログベースメトリクスは厳格で、必須ラベルが揃っていないログは「不完全なリソース」とみなしてエラーも出さずに集計対象から除外してしまいます

これらの必須ラベルが揃っていないとMonitoringやMetrics Explorerで表示できないので注意！

## Claude Code: OTEL_LOG_TOOL_DETAILS

https://code.claude.com/docs/ja/monitoring-usage

- `mcp_tool_name`を出すにはこれが必要
- 具体的に利用しているMCPのツール名

##  Playwright: Device Descriptor

- Playwrightで事前定義されている、デバイスの事前定義セット
- 「Desktop Chrome」「iPhone 15」「Pixel 7」など100種類以上
  - 実態はJSON
- Playwrightから外部ページ（e.g. Googleログインページ）にアクセスする際のUAなどを偽装できる

```json
{
  "Desktop Chrome": {
    "userAgent": "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/.../Safari/537.36",
    "screen": { "width": 1920, "height": 1080 },
    "viewport": { "width": 1280, "height": 720 },
    "deviceScaleFactor": 1,
    "isMobile": false,
    "hasTouch": false,
    "defaultBrowserType": "chromium"
  }
}
```

## ⭐️GASからVertex AI経由でAIモデルを呼び出す

1. Google Cloud (GCP) 側の準備
   ① GCPコンソールで「Vertex AI API」を有効化する。
   ② 「Model Garden」で利用したいClaudeモデルを検索し、有効化（同意）する。
   ③ GCPのダッシュボードから「プロジェクトID」を控える。
   ④ 「IAM と管理」から、GASを実行するユーザーのアカウントに「Vertex AI ユーザー」のロールを付与する。

2. GAS 側の設定
   ① GASの「エディタ」画面左側にある「サービス ＋」をクリックする。
   ② 一覧から「Vertex AI API」を選択して「追加」を押す。

3. GASの実装
   参考: https://developers.google.com/apps-script/advanced/vertex-ai?hl=ja
   参考: https://developers.google.com/apps-script/quickstart/vertex-ai?hl=ja

## [Playwright] screenshot: "only-on-failure"

参考: https://playwright.dev/docs/test-use-options

- Playwrightでテストが失敗したときにスクリーンショットを撮る設定

```ts
import { defineConfig } from '@playwright/test';

export default defineConfig({
  use: {
    // Capture screenshot after each test failure.
    screenshot: 'only-on-failure',

    // Record trace only when retrying a test for the first time.
    trace: 'on-first-retry',

    // Record video only when retrying a test for the first time.
    video: 'on-first-retry'
  },
});
```

デバッグに使えそう。どんな画面で処理がストップ・失敗しているか。

## [GitHub Actions] ::add-mask::

参考: https://zenn.dev/hankei6km/articles/add-mask-command-in-github-actions

Actions上で秘匿情報をマスクしたい時に利用できる記法
